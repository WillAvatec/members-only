extends layout
block content
    h1 Are you sure, you want to delete the post?
    form(action="" method="POST")
        input(type="hidden" name="value" required="true" value=post)
        button(type="submit") YES
        a(href="/") NO